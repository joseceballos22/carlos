BlockEvents.rightClicked(event => {
  if(event.block.id != "botanypots:terracotta_hopper_botany_pot") {
	  return
  }
  if(event.item.id == "gag:time_sand_pouch") {
	  event.cancel()
  }
})
BlockEvents.rightClicked(event => {
  if(event.block.id != "botanypots:terracotta_botany_pot") {
	  return
  }
  if(event.item.id == "gag:time_sand_pouch") {
	  event.cancel()
  }
})
BlockEvents.rightClicked(event => {
  if(event.block.id != "botanypotstiers:elite_terracotta_hopper_botany_pot") {
	  return
  }
  if(event.item.id == "gag:time_sand_pouch") {
	  event.cancel()
  }
})
BlockEvents.rightClicked(event => {
  if(event.block.id != "botanypotstiers:elite_terracotta_botany_pot") {
	  return
  }
  if(event.item.id == "gag:time_sand_pouch") {
	  event.cancel()
  }
})
BlockEvents.rightClicked(event => {
  if(event.block.id != "botanypotstiers:ultra_terracotta_hopper_botany_pot") {
	  return
  }
  if(event.item.id == "gag:time_sand_pouch") {
	  event.cancel()
  }
})
BlockEvents.rightClicked(event => {
  if(event.block.id != "botanypotstiers:ultra_terracotta_botany_pot") {
	  return
  }
  if(event.item.id == "gag:time_sand_pouch") {
	  event.cancel()
  }
})
BlockEvents.rightClicked(event => {
  if(event.block.id != "botanypotstiers:creative_terracotta_hopper_botany_pot") {
	  return
  }
  if(event.item.id == "gag:time_sand_pouch") {
	  event.cancel()
  }
})
BlockEvents.rightClicked(event => {
  if(event.block.id != "botanypotstiers:creative_terracotta_botany_pot") {
	  return
  }
  if(event.item.id == "gag:time_sand_pouch") {
	  event.cancel()
  }
})
